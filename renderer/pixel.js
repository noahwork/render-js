class NoahPixel extends NoahObject {
    // 表示顶点的类, 包含 NoahVector 和 NoahColor
    // 表示了一个坐标和一个颜色
    constructor(position, color) {
        super()
        this.position = position
        this.color = color
    }
}
